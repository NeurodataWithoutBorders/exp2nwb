This folder contains a "toy" example that illustrates usage of the code stored in the folder src.

The example is stored as a Jupyter notebook and can be run using Jupyter application.

It generates several partial NWB files and then assembles them to produce a full-size NWB file. After the assembly, a validation check is performed.


